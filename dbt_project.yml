name: 'ctt_winter_series'
version: '1.0.0'

profile: 'ctt_winter_series'

model-paths: ["_models"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  ctt_winter_series:
    staging:
      +schema: staging
      +materialized: table
    intermediate:
      +schema: intermediate
      +materialized: table
    core:
      +schema: core
      +materialized: table